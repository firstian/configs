# Update the hostname
sudo scutil --set HostName <name>
sudo scutil --set LocalHostName <name>
sudo scutil --set ComputerName <name>

# If we need to generate new keys
ssh-keygen -t ed25519

# After copying over all the keys, add them to the ssh agent
ssh-add <list of keys>

# Check the keys are added:
ssh-add -L

# install xcode tools to get git
xcode-select --install

# Install OMZ before stowing all the dotfiles to make sure all the code is available
# when we source the dotfile.
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# Clone and set up config
git clone https://github.com/firstian/configs

# Install homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
eval "$(/opt/homebrew/bin/brew shellenv)"

# Install some basic stuff to bootstrap
brew install bash bat delta fd fzf gotop htop just stow tree wget xz

# Run the setup script in a subshell to preserve cwd, need stow.
(cd configs && ./setup_dotfiles.sh && source ~/.zshrc)

# Stuff needed by nvim
brew install ripgrep jesseduffield/lazygit/lazygit
brew search '/font-.*-nerd-font/' | awk '{ print $1 }' | xargs brew install --cask
brew install neovim

# Set up Vim
# git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
# vim +PluginInstall

# Install more stuff from homebrew
brew install awscli gh db-browser-for-sqlite iterm2 jq graphviz git-lfs
brew install --cask ghostty obsidian visual-studio-code rectangle brave-browser
brew install --cask zoom slack docker
brew install --cask discord onyx signal

# Set up dev tools
curl -LsSf https://astral.sh/uv/install.sh | sh
brew install asdf

asdf plugin add python
asdf install python 3.14.50
asdf set python 3.14.0

# asdf plugin-add poetry https://github.com/asdf-community/asdf-poetry.git
# asdf install poetry 1.8.4
# asdf set poetry 1.8.4

asdf plugin add nodejs
asdf install nodejs 24.5.0
asdf set nodejs 24.5.0

brew install pnpm
pnpm setup

# Disable press&hold so command mode for vim plugin can repeat command in VS Code
defaults write NSGlobalDomain ApplePressAndHoldEnabled -bool false

# Manual changes in System Preferences:
- Keyboard
  - Modifier key change Caps Lock to Esc
  - Change Mission control shortcuts from Cmd-arrow to Cmd-Option-arrow


